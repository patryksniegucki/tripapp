const Place = require('../models/Place')
const Photo = require('../models/Photo')

exports.getPhotosForPlace = (req, res) => {
    res.sendStatus(500);
}

exports.addPhotoForPlace = (req, res) => {
    res.sendStatus(500);
}

exports.removePhotoById = (req, res) => {
    //Photo.remove({
    //    _id = //found photo id
    //})
    //res.sendStatus(500);
}